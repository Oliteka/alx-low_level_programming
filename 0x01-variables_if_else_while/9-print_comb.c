#include <stdio.h>
/**
 * main- is the data entry point.
 * Return: 0 if success and non zero when error.
 */

int main(void)
{
	int n = '0';

	while (n <= '9')
	{
		putchar(n);
		if (n != '9')
		{
			putchar(',');
			putchar(' ');
		}
		n++;
	}
	putchar('\n');

	return (0);
}
