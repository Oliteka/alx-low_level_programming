#include <stdio.h>
/**
 * main - is the program entry point.
 * Return: 0 if success and non zero if error.
 */

int main(void)
{
	int i;

	for (int i = '0'; i <= '9'; i++)
	putchar(i);
	putchar('\n');

	return (0);
}
