#include <stdio.h>
/**
 * main - is the program entry point.
 * Return: 0 if succes, non-zero if error.
 */

int main(void)
{
	char i;

	for (i = 'z'; i >= 'a'; i--)
	putchar('i');
	putchar('\n');

	return (0);
}
